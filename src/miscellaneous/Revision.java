package miscellaneous;

public class Revision {

    public static void main(String[] arg) {
    }

    private static int findMedian(int[] v) {
        int len=v.length;

        if(len%2==0) {
            //if number of elements is even -> ava of mid 2 elements

        } else {
            //if number of elements is even -> mid elements

        }
        return 0;
    }

    // Try with both Quick and Merge sort
    private static void sortArray(int arr[]) {
        int start=0;
        int end=arr.length-1;

        int mid=(start+end)/2;



    }



}
